Split changed files into separate commits by logical unit.

Rules:
- Write commit messages as a single line in Korean (e.g. "feat: 내역 수정 기능 추가")
- Use conventional commit prefixes (feat, fix, refactor, style, docs, chore, etc.)
- Never include Co-Authored-By
- Group related files into a single commit
- Verify results with git log --oneline after committing
